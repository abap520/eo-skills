# 模块 spec 固定模板

eo-spec 在 `eo-doc/dev/<module-name>/` 下按下方模板写入两份文件：**spec.md**（模块能力基线活文档）与 **spec-history.md**（归档流水，从 spec.md 剥离以控制 spec 体量）。

```markdown
---
title: <模块名称>需求规格说明
module_name: <module-name>
tags: [标签1, 标签2, 标签3]
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: draft
summary: >
  一句话核心摘要，描述该模块的职责。
---

# <模块名称> 需求规格说明

> 状态：草稿 | 已确认（活文档，随 change 归档持续演化）
> 创建日期：YYYY-MM-DD
> 所属里程碑：如项目使用里程碑则填写，否则写"无"

## 1. 概述

### 1.1 模块简述
一句话说清楚这个模块做什么。

### 1.2 所属系统
标明属于哪个系统、子系统、包、程序集、服务或运行时区域。

### 1.3 关联文档
列出关联的 PRD 章节、roadmap、设计稿、相关模块 spec。

### 1.4 模块架构图

> 用 `flowchart LR` 画本模块的内部组件关系与对外依赖。**活文档**：change 归档时由 eo-archive 同步更新。
> 规范见 [eo-doc-manager/references/mermaid.md](../../../../eo-doc-manager/references/mermaid.md)。

```mermaid
flowchart LR
    %% 本模块内部组件用实线框；依赖的外部模块用 :::extern
    classDef extern fill:#e9ecef,stroke:#6c757d,stroke-dasharray:5 5
```

## 2. 用户故事与场景

### 2.1 目标用户
明确这个模块服务于谁。

### 2.2 用户故事
使用 "作为 <角色>，我希望 <行为>，以便 <价值>" 格式。

### 2.3 使用场景
描述 2-3 个典型使用场景，包含前置条件和期望结果。

## 3. 功能需求

### 3.1 输入
明确所有输入来源、格式、必填/选填。

### 3.2 输出
明确所有输出目标、格式、成功/失败的结果。

### 3.3 核心行为描述
用自然语言逐条描述模块在每个场景下的具体行为。
**禁止使用代码或伪代码。**

### 3.4 业务规则与约束
列出所有业务逻辑规则、限制条件、异常处理要求。

### 3.5 核心流程图 / 状态图

> **必画条件**：模块涉及用户操作流程、业务状态流转、或多角色交互。纯计算/查询型模块可写"无"。
> 图类型选择与规范见 [eo-doc-manager/references/mermaid.md](../../../../eo-doc-manager/references/mermaid.md)。
> 活文档：change 归档时由 eo-archive 用变更后的流程图覆盖这里（并清除 `:::new/:::changed` 标注）。

```mermaid
flowchart TD
    %% 示例；按实际业务替换
```

## 4. 非功能需求

### 4.1 性能要求
如有明确指标则列出，无则标注"无特殊要求"。

### 4.2 安全要求

### 4.3 兼容性要求

## 5. 边界与限制

### 5.1 明确包含（In Scope）
逐条列出本模块范围内的内容。

### 5.2 明确排除（Out of Scope）
逐条列出本模块明确不做的内容。

### 5.3 已知约束
列出技术或业务上的已知限制。

## 6. 验收标准（Acceptance Criteria）

使用 "Given-When-Then" 格式逐条列出：
- **Given** <前置条件>
- **When** <用户操作>
- **Then** <期望结果>

## 7. 多层协作需求（Multi-Layer Alignment）

> **条件章节**：仅在 `eo-doc/templates/spec-layers.md` 存在时生成。
> 章节结构由模板定义。若无模板，跳过此章节，后续章节编号顺延。

### 7.1 交互流向
描述本模块中各层之间的数据/控制流方向。
格式：`层A → 层B：做什么`，逐条列出。

### 7.2 各层职责划分

| 层 | 本模块中的职责 | 不做什么 |
|----|---------------|---------|

### 7.3 需要的数据与通信

| 数据/消息 | 提供方 | 消费方 | 格式要求 | 备注 |
|-----------|--------|--------|---------|------|

### 7.4 一致性检查清单

逐条列出各层之间必须保持一致的约束：
- [ ] 约束描述

> **禁止事项**：此章节只描述需求层面的协作关系。
> 不写接口签名、消息字段、代码结构。
> 不指定具体技术方案，只说明"需要什么"而非"怎么实现"。

## 8. 开放问题（Open Questions）

列出所有尚未解决的问题，标注负责人和期望解决日期。
如全部已解决则标注"无"。

---

> **变更历史**：本模块的「关联变更」与「变更记录」见 [spec-history.md](spec-history.md)（与 spec.md 同目录）。
> 该文件由 eo-spec 在模块初始化时创建、由 eo-archive 在每次归档时自动追加。
```

## 变更历史文件 spec-history.md

「关联变更」「变更记录」是随 change 单调增长的归档流水——只增不减、对"模块当前能做什么"零贡献。把它们留在 spec.md 正文里会让活文档持续膨胀、最终超出单次读取上限。因此剥离到同目录的 `spec-history.md`：

- **eo-spec** 在模块初始化时按下方模板创建它（含「模块初始化」初始记录行）。
- **eo-archive** 在每次归档时往它的两张表各追加一行，不再写 spec.md。

```markdown
---
title: <模块名称> 变更历史
module_name: <module-name>
updated: YYYY-MM-DD
---

# <模块名称> 变更历史

> 本文件由 eo-spec 创建、eo-archive 在每次归档时自动维护。**请勿手工编辑。**
> 模块当前能力基线见 [spec.md](spec.md)。

## 关联变更

> 每归档一个 change，eo-archive 在此表追加一行。

| 变更 | 日期 | 摘要 |
|------|------|------|

## 变更记录

| 日期 | 变更内容 | 变更人 |
|------|---------|--------|
| YYYY-MM-DD | 模块初始化 | eo-module-init |
```
